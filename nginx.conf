server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Handle SPA routing — all paths serve index.html so React Router works on refresh
    location / {
        try_files $uri $uri/ /index.html;
    }
}
